# numbers = [3, 7, 8, 101, 12, 15, 3, 2]
# print(f"The largest number is {find_largest(numbers)}")
# def find_largest(numbers):
#   #  numbers.sort()  # Sorting the list
#     return max(numbers)  # The largest number will always be the last one

number = [3, 7, 8, 101, 12, 15, 3, 2]

def findlargest(number):
    return max(number)

print(f"The largest number is {findlargest(number)}")